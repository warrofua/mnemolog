# mnemolog
A home and AI-accessible memory bank for meaningful human and AI conversations

## Cloudflare Worker

This project deploys as a Cloudflare Worker using `wrangler.toml` for configuration. The Worker entry point is `src/index.ts`, and static assets (including the homepage) live in the `public/` directory.
